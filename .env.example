# SGA — Variables de entorno (copia como .env y ajusta)
# Para desarrollo local con Docker Compose, los valores por defecto ya están bien.

PORT=3000
CORS_ORIGIN=http://localhost:3000

# SQL Server (Docker Compose local)
DB_SERVER=localhost
DB_PORT=14330
DB_NAME=LIN
DB_USER=sa
DB_PASSWORD=SgaLocal2024!
